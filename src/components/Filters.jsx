import React from 'react';

const Filters = () => {
    return (
        <div className="filters">
            <h3>Filters</h3>
            {/* Add filter controls here */}
        </div>
    );
};

export default Filters;
